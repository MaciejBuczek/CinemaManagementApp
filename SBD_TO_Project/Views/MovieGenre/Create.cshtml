@model SBD_TO_Project.Models.ViewModels.MovieGenreVM

<form method="post" asp-action="Create">
    <input asp-for="IdMovie" hidden />
    <div class="border p-3">
        <div class="form-group row">
            <h2 class="text-dark pl-3">
                Add Genres To Movie
            </h2>
        </div>
        <div class="row">
            <div class="col-8 offset-2">
                @if (Model.GenreCheckBoxList.Count > 0)
                {
                    <div class="form-group row">
                        <table class=" table table-bordered table-striped" style="width:100%">
                            <thead>
                                <tr>
                                    <th style="width:20%">
                                        Is selected
                                    </th>
                                    <th>
                                        Genre Name
                                    </th>
                                </tr>
                            </thead>
                            @for (int i = 0; i < Model.GenreCheckBoxList.Count(); i++)
                            {
                                @Html.HiddenFor(g => g.GenreCheckBoxList[i].Id)
                                <tr>
                                    <td>
                                        @Html.CheckBoxFor(g => g.GenreCheckBoxList[i].IsChecked)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(g => ((Genre)g.GenreCheckBoxList[i].Object).Name)
                                    </td>
                                </tr>
                            }
                        </table>
                    </div>
                }
                else
                {
                    <p>All possible genres are already assigned to this movie</p>
                }
                <div class="form-group row">
                    <div class="col-8 offset-2 row">
                        @if (Model.GenreCheckBoxList.Count > 0)
                        {
                            <div class="col">
                                <input type="submit" class="btn btn-info w-100" value="Add" />
                            </div>
                        }
                        <div class="col">
                            <a asp-route-Id="@Model.IdMovie" asp-action="Index" class="btn btn-primary w-100">Back</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>